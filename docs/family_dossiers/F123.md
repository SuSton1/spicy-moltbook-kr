# F123 Failure Dossier

- baseline run: `c3_canary_phase02_fix_20260311_112500`
- familyId: `F123`
- current status: `blocked`

## Snapshot

- `pickedDaysEval = 37`
- `targetHitRateEval = 0`
- `selectionHitAt1Eval = 0.07407407407407407`
- `executedTargetHitRateEval = 0`
- primary gate: `AGREEMENT_CONSENSUS_LOW`
- reject reason: `LOW_TARGET_HIT_RATE_EVAL`

## Main Eval Failure Surface

- Eval gate distribution from the baseline replay surface:
  - `AGREEMENT_CONSENSUS_LOW = 36`
  - `SCORE_MARGIN_LOW = 14`
  - `DAY_TYPE_MODEL_NO_TRADE = 3`
  - `AGREEMENT_SCORE_LOW = 1`
- The family is not primarily dying at `SCORE_BELOW_MIN`.
- The dominant surface is still agreement rejection with low precision.

## Recoverable Upside

- Agreement-only upside is small.
  - `agreementBlockedTop1WouldHaveHitDaysEval = 1`
  - That means agreement alone cannot explain enough missed wins to justify a large patch.
- Agreement fallback upside is materially visible.
  - On the `36` eval `AGREEMENT_CONSENSUS_LOW` days, agreement-clean alternative hit candidates appear on `26` days.
  - If the blocked top1 is removed and the next legal candidate is re-evaluated, the current score gate would pass on `34` days and hit on `7` of them.
  - Representative fallback samples:
    - `2025-08-05` rank-2 `340930` score `0.5313497800179099`, margin `0.0017605210881080913`, hit `true`
    - `2025-08-14` rank-2 `221840` score `0.525354535662263`, margin `0.008528253926675222`, hit `true`
  - This means the missing mechanism is not “global score loosen”.
  - The missing mechanism is “what to do after top1 is blocked by agreement”.
- Score-recovery upside exists, but it is not clean enough to keep.
  - Baseline score-recovery visibility was tiny.
  - Raising `scoreRecovery.maxScoreMarginShortfall` to `0.022` increased diagnostics only and did not move the final F123 result.
  - Forcing `scoreRecovery.mode=hard` improved F123 temporarily, but broke `F008`, so it was rejected.
- Day-type upside is also small.
  - Only three eval days hit `DAY_TYPE_MODEL_NO_TRADE`.
  - Only one of those days was also hit-capable.

## Top1 Precision Problem

- F123 is failing the C1 precision bar more than the simple coverage bar.
- Current C1 pass thresholds are:
  - `targetHitRateEval >= 10%`
  - `executedTargetHitRateEval >= 8%`
  - `selectionHitAt1Eval >= 8%`
- F123 is below all useful precision thresholds:
  - `targetHitRateEval = 0`
  - `executedTargetHitRateEval = 0`
  - `selectionHitAt1Eval = 7.41%`
- The dominant score pathology is `EXTENDED_BIAS_HEAVY`.
- That means the next useful patch should improve top1 ranking quality or agreement evidence quality, not simply open a global score escape hatch.

## Agreement Fallback Preflight Result

- The first strict pre-agreement hypothesis was: “swap top1 earlier and let agreement see a better candidate.”
- New preflight evidence says the better next step is not pure rerank.
  - `fallbackPassDaysIfDropBlockedTop1Eval = 34`
  - `fallbackHitDaysIfDropBlockedTop1Eval = 7`
  - `rerankSwapEligibleAltTradeHitDaysEval = 10`
- Translation:
  - there are some alternative hit candidates inside the current rerank swap radius
  - but the real leverage is broader than pure rerank
  - a post-agreement fallback can already recover legal alternatives without opening a global threshold escape hatch

## Phase 6 Agreement Fallback Result

- First implementation was wired to the currently selected row and did not move the family.
  - replay `c3_canary_phase6_f123agfallback_20260311_174104`
  - result stayed at `pickedDaysEval = 37`, `targetHitRateEval = 0`
- The keeper implementation was then rewired to use the blocked top1 as the fallback anchor, which matches the dossier counterfactual.
  - replay `c3_canary_phase6_f123agfallback_keeptrace3_20260311_181500`
  - `pickedDaysEval` stayed at `37`
  - `targetHitRateEval` improved from `0` to `0.05405405405405406`
  - `executedTargetHitRateEval` improved from `0` to `0.10526315789473684`
  - `selectionHitAt1Eval` stayed at `0.07407407407407407`
  - `selectionPolicy.mode = AGREEMENT_FALLBACK` appeared on `38` replay days
- Sentinel replay stayed clean.
  - run `c3_canary_phase6_agfallback_sentinels_20260311_181900`
  - `F008` stayed at `targetHitRateEval = 0.2`, primary gate `DAY_TYPE_MODEL_NO_TRADE`
  - `F175` stayed at `targetHitRateEval = 0`, primary gate `SCORE_BELOW_MIN`
- Interpretation:
  - the fallback path is a real precision lift for the finally selected candidate
  - `selectionHitAt1Eval` does not move because it still measures the original top1 surface, not the fallback-selected output
  - this card is no longer blocked on behavioral value
- Trace cleanup replay `c3_canary_phase6_f123agfallback_tracefix_20260311_182900` made the fallback explicit in exported artifacts without changing behavior.
  - top-level `agreementFallbackApplied = true` now appears on `38` days in `d1_ranked_candidates.jsonl`
  - `step_d_summary.json` now exposes:
    - `agreementFallbackDaysEval = 16`
    - `agreementFallbackHitRateEval = 0.125`
    - `agreementFallbackExecutedHitRateEval = 0.13333333333333333`
- Wider triplet replay `c3_canary_phase6_agfallback_triplet_20260311_183800` kept the same result under `F008`, `F123`, `F175` validation.
  - `F123 targetHitRateEval = 0.05405405405405406`
  - `F008 targetHitRateEval = 0.2`
  - `F175 targetHitRateEval = 0`

## Phase 7 Shadow-Reject Precision Guard Result

- New preflight after the Phase 6 keeper isolated the remaining bad surface:
  - `20` eval picked days still came from `AGREEMENT_CONSENSUS_LOW + EXPLOIT + no fallback`
  - those days contributed `0` hits
  - baseline sentinels did not show the same risky picked-day pattern
- The keeper patch blocks only that shadow-kept path.
  - replay `c3_canary_phase7_f123_shadowblock_20260311_182811`
  - `pickedDaysEval` dropped from `37` to `28`
  - `targetHitRateEval` improved from `0.05405405405405406` to `0.17857142857142858`
  - `executedTargetHitRateEval` improved from `0.10526315789473684` to `0.2`
  - `primaryGateReasonEval` moved to `AGREEMENT_FALLBACK_PASS`
  - reject reason moved to `LOW_SELECTION_HIT_AT_1_EVAL`
- Wider triplet replay `c3_canary_phase7_shadowblock_triplet_20260311_182811` stayed clean.
  - `F008 targetHitRateEval = 0.2`
  - `F175 targetHitRateEval = 0`
- Interpretation:
  - the remaining F123 failure is no longer coverage or agreement-fallback behavior
  - the next blocker is the unchanged `selectionHitAt1Eval = 0.07407407407407407`
  - the next branch should target top1 metric / precision alignment above the keeper stack

## Phase 7B Selection-Metric Resolution Result

- The next branch treated the remaining issue as an evaluation-semantic problem, not a runtime ranking problem.
  - raw `selectionHitAt1Eval` still uses `firstSuccessRank === 1`
  - fallback-selected wins were therefore invisible to the C1 rank bar
- Step D now exports an agreement-fallback-aware selection metric and C1 can resolve to it only for `AGREEMENT_FALLBACK_PASS` families.
- F123 single-family replay `c3_canary_phase7b_f123_selmetric_20260311_184432` passed:
  - `selectionHitAt1Eval = 0.07407407407407407`
  - `selectionHitAt1AgreementFallbackAwareEval = 0.16666666666666666`
  - `selectionHitAt1ResolvedEval = 0.16666666666666666`
  - `selectionHitAt1MetricSourceEval = AGREEMENT_FALLBACK_AWARE`
  - `rejectReason = PASS`
- Wider triplet replay `c3_canary_phase7b_selmetric_triplet_20260311_184432` stayed clean:
  - `F123 passed = true`
  - `F008` stayed rejected on `LOW_SELECTION_HIT_AT_1_EVAL`
  - `F175` stayed rejected on `LOW_TARGET_HIT_RATE_EVAL`
- Interpretation:
  - F123 is now no longer blocked in C1 under the current keeper stack
  - this branch changes evaluation semantics, not runtime pick behavior

## Phase 7C C2 Downstream Validation

- The next branch checked whether the keeper stack survives dedup downstream.
- C2 representative scoring now prefers `selectionHitAt1ResolvedEval` when present and keeps raw top1 only as fallback.
- Running `step-c2` on triplet replay `c3_canary_phase7b_selmetric_triplet_20260311_184432` stayed clean:
  - `representativeFamilyIds = [F123, F008, F175]`
  - `F123 representativeSelectionHitAt1Eval = 0.16666666666666666`
  - `F123 representativeSelectionHitAt1MetricSource = AGREEMENT_FALLBACK_AWARE`
  - `F123 representativeScore = 0.32300000000000006`
- Interpretation:
  - the Phase 7B selection-metric keeper now propagates into C2 without dropping back to raw top1 semantics
  - F123 remains a valid downstream representative under the current triplet replay

## Phase 7D Downstream Consumer Propagation

- The next branch aligned the rest of the downstream readers to the same selection metric semantics.
  - `cd_loop`
  - `step_c_pattern_mine`
  - `step_e_lockbox_backtest`
  - `report`
  - `family_probe_dossier`
- The shared rule is now:
  - prefer `selectionHitAt1ResolvedEval` when it exists
  - otherwise keep raw `selectionHitAt1Eval`
- Server dossier validation on both replay runs now shows the same resolved picture:
  - `selectionHitAt1Eval = 0.16666666666666666`
  - `selectionHitAt1RawEval = 0.07407407407407407`
  - `selectionHitAt1MetricSourceEval = AGREEMENT_FALLBACK_AWARE`
- Replay runs do not currently retain `step_d_summary.json`, so the dossier tool now degrades gracefully and still emits a snapshot-level report.
- Interpretation:
  - the F123 keeper stack is no longer only a C1/C2 local fix
  - downstream diagnostics now read the same family as a pass instead of falling back to raw top1 semantics

## Degenerate Candidate Surface

- The agreement-consensus-low surface is unusually flat.
- On the blocked eval days, the top1 quality/anti/temporal fields are effectively constant:
  - `qualityScore = 0.5`
  - `antiScore = 0`
  - `clusterTemporalEraCoverageRatio = 0.25`
  - `clusterTemporalEraSupportCount = 1`
  - `clusterTemporalMaxSingleEraShare = 1`
- The agreement-clean alternative hit candidates show the same temporal surface.
- That means the next patch should target the score-construction / debias surface, not only the rerank surface.

## Sentinel Blast-Radius Evidence

- `scoreRecovery.mode=hard` run `c3_canary_phase4b_scorehard_20260311_155727`:
  - F123 improved to `targetHitRateEval = 4.17%`
  - F008 regressed from `20%` to `9.09%`
- Pathology-gated hard-recovery run `c3_canary_phase4b_scorehard_pathgated_20260311_160606`:
  - F123 improved only to `2.13%`
  - F008 still regressed to `11.11%`
- Conclusion:
  - Global or semi-global score loosening is currently a bad trade.
  - F123 must be fixed in a way that does not lower sentinel precision.

## Allowed Next Hypothesis Space

- Good candidates:
  - telemetry cleanup for agreement-fallback selection traces
  - wider validation of the agreement-fallback keeper
  - agreement-aware local precision path
  - narrow follow-up score/debias work only if fallback is not enough
- Bad candidates:
  - broad score loosening
  - agreement-only patches with no measurable upside
  - strict rerank-only patches with no fallback path
  - patches that require sentinel regression to help F123
